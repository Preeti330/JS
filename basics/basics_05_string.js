//data stores between "" /'' is called string . 
//and this is primitive type data stoores in stack 
//string which created with literals '' is immutable i.e data modification or changes on same string wont happen instaed its create new copy of string.
//if string created with object is mutable , i.e manipulation on straing is easy 
//some of string methods 
//length-> gives length , endWith()->checks the staring which endwith perticular later,stratWIth
//charAT()-> takes only positive , at()->takes nagivte too , trim()
//split -> gives array , substring(index,lengt);slicle(index,lenght) 
//toUpperCase,toLowercase

let username="preeti";
console.log(username.toUpperCase);
console.log(username.toLowerCase);
console.log(username.endsWith('ti'));
console.log(username.length);